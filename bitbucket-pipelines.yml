# Bitbucket Pipelines is intentionally disabled for now.
# Deployments are handled only from GitHub Actions.

pipelines:
  custom:
    disabled:
      - step:
          name: Bitbucket pipelines disabled
          script:
            - echo "Bitbucket Pipelines disabled. Deploy from GitHub Actions only."
